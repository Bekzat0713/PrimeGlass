{
  "name": "prime-glass-site",
  "version": "1.0.0",
  "description": "Корпоративный сайт Prime Glass Technologies на HTML/CSS/JS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "test": "node validate.js && node analytics.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "express": "^4.18.4",
    "express-rate-limit": "^7.0.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
